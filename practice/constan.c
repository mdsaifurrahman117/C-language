#include <stdio.h>
int main () {
            const int height = 5;
            const float pi = 3.14;
            const char letter = 'A';
            printf ("value of height : %d", height);
            printf ("\n value of pi : %f", pi);
            printf ("\n value of letter : %c", letter); 

            return 0;
}
