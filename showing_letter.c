#include <stdio.h>
int main () {
            char letter;
            printf ("enter a character : ");
            scanf ("%c", &letter);
            printf ("your entered a character : %c", letter);
            return 0;
}